Flask==2.2.3
Flask-Cors==3.0.10
Pillow==11.1.0
torch==2.7.0         # Make sure this version matches what you used for training/saving model
torchvision==0.22.0 # Match the torch version
gunicorn==23.0.0      # Production WSGI server
numpy               # Torch dependency, include explicitly
requests            # Often useful, included by Flask indirectly too sometimes
urllib3             # requests dependency
idna                # requests dependency
charset-normalizer  # requests dependency
certifi             # requests dependency
Werkzeug==2.2.3     # Flask dependency (often good to pin)
itsdangerous==2.2.0 # Flask dependency (often good to pin)
click==8.1.8        # Flask dependency (often good to pin)