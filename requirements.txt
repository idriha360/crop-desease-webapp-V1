Flask==2.2.3
flask-cors==3.0.10
Werkzeug<2.3.0
Pillow>=9.4.0 # You have Pillow with a version, let's keep it pinned or allow newer
gunicorn

# For PyTorch CPU - this method should work with recent pip versions
# It tells pip to also look at the PyTorch CPU-specific wheel index
torch --extra-index-url https://download.pytorch.org/whl/cpu
torchvision --extra-index-url https://download.pytorch.org/whl/cpu