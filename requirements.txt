Flask==2.2.3
flask-cors==3.0.10
Werkzeug<2.3.0
Pillow==10.1.0
gunicorn==22.0.0

# --- PyTorch CPU-ONLY (targeting versions as of April 23, 2025 example) ---
# For these newer versions, the standard manylinux wheels are CPU-only.
# The -f flag in the Dockerfile's pip install will further ensure CPU source.
torch==2.7.0
torchvision==0.22.0