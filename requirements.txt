Flask==2.2.3
flask-cors==3.0.10
Werkzeug<2.3.0
Pillow==10.1.0
gunicorn==22.0.0

# Core ML libraries - versions and CPU source will be handled in Dockerfile
torch
torchvision
# Add 'torchaudio' here ONLY if your app.py or model_utils.py actually imports and uses it.
# Based on your shared code, you likely DO NOT need torchaudio.